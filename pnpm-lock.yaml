// NIET op Edge draaien en niet cachen (anders HTML/redirects)
export const runtime = 'nodejs';
export const dynamic = 'force-dynamic';
export const fetchCache = 'force-no-store';

import NextAuth from 'next-auth';
import type { NextAuthOptions } from 'next-auth';
import CredentialsProvider from 'next-auth/providers/credentials';

const authOptions: NextAuthOptions = {
  debug: true,
  secret: process.env.NEXTAUTH_SECRET,    // v4 gebruikt NEXTAUTH_SECRET
  session: { strategy: 'jwt' },
  providers: [
    CredentialsProvider({
      name: 'Demo',
      credentials: { user: { label: 'User', type: 'text' } },
      async authorize() {
        // geen echte login nodig; endpoints geven zo wél geldige JSON
        return null;
      },
    }),
  ],
};

const handler = NextAuth(authOptions);
export { handler as GET, handler as POST };
